# Work SSH key first (default for github.com)
[[ssh-keys]]
item = "Causaly: SSH Key"
vault = "Work"
account = "Ross Timson"

# Personal keys second (use github-personal host alias)
[[ssh-keys]]
vault = "Private"
account = "Our Family"
