#!/bin/sh

# Helper script for removing all entries for a host from known_hosts file.
#
# Usage:
#
#    nukehost <hostname>
#

if [ -z "$1" ]; then
	echo "Usage: nukehost <hostname>" >&2
	exit 1
fi

ssh-keygen -R "$1"
